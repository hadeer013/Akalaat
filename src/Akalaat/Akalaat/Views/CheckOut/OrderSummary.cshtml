@{
    ViewData["Title"] = "Order Summary";
}

<main class="main">

    <nav aria-label="breadcrumb" class="breadcrumb-nav">
        <div class="container">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="index.html">Home</a></li>
                <li class="breadcrumb-item"><a href="#">Cart</a></li>
                <li class="breadcrumb-item active" aria-current="page">Your Order</li>
            </ol>
        </div><!-- End .container -->
    </nav><!-- End .breadcrumb-nav -->

    <div class="checkout" style="margin: 0% 20%; ">
        <div class="container">
            <form action="Checkout" method="post">
                <div class="row">
                    <aside style="width:80%">
                        <div class="summary">
                            <h3 class="summary-title">Your Order</h3><!-- End .summary-title -->

                            <table class="table table-summary">
                                <thead>
                                    <tr>
                                        <th>Dish</th>
                                        <th>Total</th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (var itemName in ViewData["PurchasedItemNames"] as List<string>)
                                    {
                                        <tr>
                                            <td>@itemName</td>
                                            <td><span id="itemPrice_@itemName"></span></td>
                                        </tr>
                                    }
                                    <tr class="summary-total">
                                        <td>Total:</td>
                                        <td>$<span id="totalPrice"></span></td>
                                    </tr><!-- End .summary-total -->
                                </tbody>
                            </table><!-- End .table table-summary -->

                            <div class="accordion-summary" id="accordion-payment">

                                <div class="card">
                                    <div class="card-header" id="heading-3">
                                        <h2 class="card-title">
                                            <a class="collapsed" role="button" data-toggle="collapse"
                                               href="#collapse-3" aria-expanded="false"
                                               aria-controls="collapse-3">
                                                Cash on delivery
                                            </a>
                                        </h2>
                                    </div><!-- End .card-header -->
                                    <div id="collapse-3" class="collapse" aria-labelledby="heading-3"
                                         data-parent="#accordion-payment">
                                        <div class="card-body">
                                            Quisque volutpat mattis eros. Lorem ipsum
                                            dolor sit amet, consectetuer adipiscing elit. Donec odio.
                                            Quisque volutpat mattis eros.
                                        </div><!-- End .card-body -->
                                    </div><!-- End .collapse -->
                                </div><!-- End .card -->


                                <div class="card">
                                    <div class="card-header" id="heading-5">
                                        <h2 class="card-title">
                                            <a class="collapsed" role="button" data-toggle="collapse"
                                               href="#collapse-5" aria-expanded="false"
                                               aria-controls="collapse-5">
                                                Credit Card (Stripe)
                                                <img src="assets/images/payments-summary.png"
                                                     alt="payments cards">
                                            </a>
                                        </h2>
                                    </div><!-- End .card-header -->
                                    <div id="collapse-5" class="collapse" aria-labelledby="heading-5"
                                         data-parent="#accordion-payment">
                                        <div class="card-body">
                                            Donec nec justo eget felis facilisis
                                            fermentum.Lorem ipsum dolor sit amet, consectetuer adipiscing
                                            elit. Donec odio. Quisque volutpat mattis eros. Lorem ipsum
                                            dolor sit ame.
                                        </div><!-- End .card-body -->
                                    </div><!-- End .collapse -->
                                </div><!-- End .card -->
                            </div><!-- End .accordion -->
                        </div><!-- End .summary -->
                    </aside><!-- End .col-lg-3 -->
                </div><!-- End .row -->
                <button type="submit" class="btn btn-outline-primary-2 btn-order btn-block">
                    <span class="btn-text">Place Order</span>
                    <span class="btn-hover-text">Proceed to Checkout</span>
                </button>
            </form>
        </div><!-- End .container -->
    </div><!-- End .checkout -->

</main><!-- End .main -->
@section scripts {
    <script>
        const itemPrices = @Html.Raw(Json.Serialize(ViewData["ItemPrices"])); // Serialize the dictionary

        document.addEventListener("DOMContentLoaded", function () {
            const totalPriceElement = document.getElementById("totalPrice");
            let total = 0;

            for (const itemName in itemPrices) {
                if (itemPrices.hasOwnProperty(itemName)) {
                    const itemPriceElement = document.getElementById(`itemPrice_${itemName}`);
                    itemPriceElement.textContent = itemPrices[itemName].toFixed(2); // Handle missing prices

                    total += itemPrices[itemName];
                }
            }

            totalPriceElement.textContent = total.toFixed(2); // Format to 2 decimal places
        });
    </script>
}


@* @{
    ViewData["Title"] = "Order Summary";
}

<div class="container">
    <h2>Order Summary</h2>

    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h4>Your Order</h4>
                <ul class="list-group">
                    @foreach (var itemName in ViewData["PurchasedItemNames"] as List<string>)
                    {
                        <li class="list-group-item">
                            @itemName
                        </li>
                    }
                    <li class="list-group-item">
                        <strong>Total</strong>: $@ViewData["TotalPrice"]
                    </li>
                </ul>
            </div>

            <div class="col-md-6">
                <h4>Payment Method</h4>
                <form method="post">
                    <div class="form-check">
                        <input class="form-check-input" type="radio" id="onlinePayment" name="paymentMethod" value="onlinePayment">
                        <label class="form-check-label" for="onlinePayment">
                            Online Payment
                        </label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" id="cashOnDelivery" name="paymentMethod" value="cashOnDelivery">
                        <label class="form-check-label" for="cashOnDelivery">
                            Cash on Delivery
                        </label>
                    </div>
                    <button type="submit" class="btn btn-primary">Proceed to Payment</button>
                </form>
            </div>
        </div>
    </div>

    @section scripts {
        <script>
            // Assuming you have a way to populate these values from the server-side
            const itemPrices = {
                // Key: Item Name, Value: Price
                // ... Populate with data from ViewData or similar
            };

            document.addEventListener("DOMContentLoaded", function () {
                const totalPriceElement = document.getElementById("totalPrice");
                let total = 0;

                for (const itemName of ViewData["PurchasedItemNames"]) {
                    const itemPriceElement = document.getElementById(`itemPrice_${itemName}`);
                    itemPriceElement.textContent = itemPrices[itemName] || 0; // Handle missing prices

                    total += itemPrices[itemName] || 0;
                }

                totalPriceElement.textContent = total.toFixed(2); // Format to 2 decimal places
            });
        </script>
    } *@
