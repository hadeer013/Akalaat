@* @model Akalaat.ViewModels.DishVM *@
@model Akalaat.DAL.Models.Dish
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>DishVM</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dd class="col-sm-10">
            <img src="~/images/@Model.Dish_image" alt="Dish Image" />
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Back to List</a>
</div>
