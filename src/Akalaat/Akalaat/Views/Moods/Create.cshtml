@* @model Akalaat.ViewModels.MoodVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create Mood</h1>

<hr />
<form asp-controller="Moods" asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="RestaurantId" class="control-label">Associated Restaurant:</label>
        <select asp-for="RestaurantId" class="form-control" asp-items="@Model.RestaurantsList">
            <option value="">Select Restaurant</option>
        </select>
        @Html.ValidationMessageFor(m => m.RestaurantId, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        <label asp-for="MoodId" class="control-label">Select Mood:</label>
        <select asp-for="MoodId" class="form-control" asp-items="@Model.MoodList">
            <option value="">Select Mood</option>
        </select>
        @Html.ValidationMessageFor(m => m.MoodId, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        <label asp-for="Name" class="control-label">Mood Name:</label>
        <input asp-for="Name" type="text" class="form-control" />
        @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
    </div>
    <button type="submit" class="btn btn-primary">Create Mood</button>
</form>

<div>
    <a asp-action="Index">Back to Mood List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

 *@
@model Akalaat.ViewModels.MoodVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create Mood</h1>

<hr />
<form asp-controller="Moods" asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="RestaurantId" class="control-label">Associated Restaurant:</label>
        <select asp-for="RestaurantId" class="form-control" asp-items="@Model.RestaurantsList">
            <option value="">Select Restaurant</option>
        </select>
    </div>
    <div class="form-group">
        <label asp-for="Name" class="control-label">Mood Name:</label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Create Mood</button>
</form>

<div>
    <a asp-action="Index">Back to Mood List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
